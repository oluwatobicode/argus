# Argus — Infrastructure

Docker Compose files and database migrations for local development and production.

---

## Plans

- [ ] `docker-compose.yml` — PostgreSQL + Redis for local dev
- [ ] `docker-compose.prod.yml` — Full production stack with TimescaleDB
- [ ] TimescaleDB raw SQL migrations (hypertables for events & transactions)
- [ ] Nginx/Caddy reverse proxy config
- [ ] Healthcheck scripts
