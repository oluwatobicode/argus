/*
 * TODO(you): buildEnvelope()
 *
 * Take a normalized error (type, value, frames) + options (level, environment,
 * release, user, tags, breadcrumbs) and produce an Envelope that passes the
 * API's Zod validator. Set `timestamp: Date.now()` here — ms contract.
 *
 * Keep it a pure function: no fetch, no globals — testable anywhere.
 */

export {};
