{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    /* DOM for Response/Headers in the middleware (universal types — the same
       shapes exist on Node 18+ and every edge runtime) */
    "lib": ["ES2020", "DOM"],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
