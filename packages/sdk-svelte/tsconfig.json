{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    /* DOM for the client entry; the server entry sticks to universal APIs so
       it also runs on edge deployments of SvelteKit */
    "lib": ["ES2020", "DOM"],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
